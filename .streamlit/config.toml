[server]
# headless=true é obrigatório em servidor (Streamlit Cloud): evita o prompt
# interativo de e-mail no boot, que trava o app e gera "connection refused".
headless = true

[browser]
gatherUsageStats = false
